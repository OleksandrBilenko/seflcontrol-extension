# seflcontrol-extension
Extension for Google Chrome browser that detect russian swear words

Uses regex to find words in typed messages and mark send buttons.

Currently works for:
- Twitter

Language: Ru
